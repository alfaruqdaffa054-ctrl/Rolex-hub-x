-- =============================================
-- Rolex Hub - Gabungan Clean Version
-- by rana_GZ77 & ZREfisch
-- No UI selector, langsung full Rayfield UI
-- =============================================

print("[Rolex Hub] Loading Gabungan Clean UI...")

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield', true))()

local Window = Rayfield:CreateWindow({
    Name = "Rolex Hub",
    LoadingTitle = "Rolex Hub",
    LoadingSubtitle = "Gabungan Edition",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "RolexHub",
        FileName = "Config"
    },
    KeySystem = false,
    Theme = "AmberGlow"  -- ganti kalau mau: Default, Ocean, Midnight, dll
})

-- =============================================
-- Tab: Home (Utama)
-- =============================================
local HomeTab = Window:CreateTab("Home", "home")

HomeTab:CreateParagraph({
    Title = "Credits",
    Content = "• rana_GZ77\n• ZREfisch\n• Rayfield Interface Suite\n• Thanks for using Rolex Hub"
})

HomeTab:CreateParagraph({
    Title = "Update Logs",
    Content = "• new selector ui\n• Update fitur ui"
})

HomeTab:CreateSection("Quick Actions")

HomeTab:CreateButton({
    Name = "Rejoin Server",
    Callback = function()
        Rayfield:Notify({
            Title = "Rejoining...",
            Content = "Sedang teleport ke server yang sama...",
            Duration = 4
        })
        task.wait(1)
        game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
    end
})

HomeTab:CreateButton({
    Name = "Close UI (Destroy Rayfield)",
    Callback = function()
        Rayfield:Notify({
            Title = "Goodbye",
            Content = "UI ditutup...",
            Duration = 3
        })
        task.wait(0.5)
        Rayfield:Destroy()
    end
})

-- =============================================
-- Tab: Choose UI
-- =============================================
local ChooseUITab = Window:CreateTab("Choose UI", "laptop")

ChooseUITab:CreateButton({
    Name = "Rayfield",
    Callback = function()
        Rayfield:Notify({
            Title = "Reloading...",
            Content = "Memuat ulang Rolex Hub (Rayfield)",
            Duration = 3
        })
        task.wait(0.8)
        Rayfield:Destroy()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/alfaruqdaffa054-ctrl/Rolex-hub-x/refs/heads/main/Rolex%20hub", true))()
    end
})

ChooseUITab:CreateButton({
    Name = "Luna",
    Callback = function()
        Rayfield:Notify({
            Title = "Switching...",
            Content = "Memuat Luna UI...",
            Duration = 3
        })
        task.wait(0.8)
        Rayfield:Destroy()
        loadstring(game:HttpGet("https://pastebin.com/raw/zytki4L2", true))()
    end
})

-- =============================================
-- Tab: Support
-- =============================================
local SupportTab = Window:CreateTab("Support", "info")

SupportTab:CreateButton({
    Name = "Copy Discord Invite",
    Callback = function()
        setclipboard("https://discord.gg/FN5Whm3mQ")
        Rayfield:Notify({
            Title = "Copied!",
            Content = "Discord link telah disalin ke clipboard",
            Duration = 4
        })
    end
})

SupportTab:CreateButton({
    Name = "Copy WhatsApp Channel",
    Callback = function()
        setclipboard("https://whatsapp.com/channel/0029VbCOJs023n3cFALVxb10")
        Rayfield:Notify({
            Title = "Copied!",
            Content = "WhatsApp channel link telah disalin",
            Duration = 4
        })
    end
})

print("[Rolex Hub] Gabungan Clean UI loaded successfully!")Title.TextSize = 20
Title.Font = Enum.Font.GothamBold

-- DESTROY BUTTON
local Destroy = Instance.new("TextButton")
Destroy.Parent = Main
Destroy.Size = UDim2.new(0,30,0,30)
Destroy.Position = UDim2.new(1,-35,0,5)
Destroy.Text = "X"
Destroy.BackgroundColor3 = Color3.fromRGB(200,50,50)
Destroy.TextColor3 = Color3.fromRGB(255,255,255)
Destroy.Font = Enum.Font.GothamBold
Destroy.TextSize = 16

local CornerX = Instance.new("UICorner",Destroy)
CornerX.CornerRadius = UDim.new(0,6)

-- BUTTON 1
local Button1 = Instance.new("TextButton")
Button1.Parent = Main
Button1.Size = UDim2.new(0.8,0,0,40)
Button1.Position = UDim2.new(0.1,0,0.35,0)
Button1.Text = "rayfield"
Button1.BackgroundColor3 = Color3.fromRGB(45,45,45)
Button1.TextColor3 = Color3.fromRGB(255,255,255)
Button1.Font = Enum.Font.GothamBold
Button1.TextSize = 16

local Corner1 = Instance.new("UICorner",Button1)
Corner1.CornerRadius = UDim.new(0,8)

-- BUTTON 2
local Button2 = Instance.new("TextButton")
Button2.Parent = Main
Button2.Size = UDim2.new(0.8,0,0,40)
Button2.Position = UDim2.new(0.1,0,0.6,0)
Button2.Text = "luna"
Button2.BackgroundColor3 = Color3.fromRGB(45,45,45)
Button2.TextColor3 = Color3.fromRGB(255,255,255)
Button2.Font = Enum.Font.GothamBold
Button2.TextSize = 16

local Corner2 = Instance.new("UICorner",Button2)
Corner2.CornerRadius = UDim.new(0,8)

-- BUTTON FUNCTIONS
Button1.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/alfaruqdaffa054-ctrl/Rolex-hub-x/refs/heads/main/Rolex%20hub",true))()
end)

Button2.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
    loadstring(game:HttpGet("https://pastebin.com/raw/zytki4L2",true))()
end)

Destroy.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- HOVER EFFECT
local function hover(btn)
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn,TweenInfo.new(0.2),{BackgroundColor3 = Color3.fromRGB(70,70,70)}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn,TweenInfo.new(0.2),{BackgroundColor3 = Color3.fromRGB(45,45,45)}):Play()
    end)
end

hover(Button1)
hover(Button2)
